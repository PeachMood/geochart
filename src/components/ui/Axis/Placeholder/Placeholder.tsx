import { type Component } from 'types';
import { type AxisProps } from 'components/ui/Axis';

export interface PlaceholderProps extends AxisProps {}

export const Placeholder: Component<PlaceholderProps> = () => {
  return null;
};
