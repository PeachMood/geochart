import { type Coordinates } from 'types';

export const DEFAULT_ID: string = 'gaussian-blur';

export const DEFAULT_POSITION: Coordinates = { x: 0, y: 0 };

export const DEFAULT_STD_DEVIATION: Coordinates = { x: 0, y: 5 };
