import { type Orientation } from 'types';

export const DEFAULT_ID: string = 'gradient';

export const DEFAULT_ORIENTATION: Orientation = 'horizontal';

export const OFFSET_PERCENT_FACTOR: number = 100;
