import { type LineStyle } from 'types';

export const DEFAULT_NAME: string = 'Кривая';

export const DEFAULT_STYLE: LineStyle = { color: '#021D38', thickness: 1, type: 'solid' };

export const DEFAULT_CONTINUITY: boolean = true;

export const RANGE_START: number = 0;
