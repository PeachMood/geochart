import { type Color } from 'types';

export const DEFAULT_TICKS_INTERVAL: number = 50;

export const DEFAULT_TICKS_LINES: number = 1;

export const RANGE_START: number = 0;

export const STROKE_COLOR: Color = 'black';

export const MAIN_TICK_LENGTH: number = 8;

export const SECONDARY_TICK_LENGTH: number = MAIN_TICK_LENGTH / 2;

export const TOP_PADDING = 1;

export const TICK_LABEL_PROPS = {
  y: 29,
  fontSize: 12,
  fontWeight: 400,
  fontFamily: 'sans-serif',
};
