import { type DepthCurve } from 'types';

export const DEFAULT_NAME: string = 'Трек глубины';

export const DEFAULT_HEIGHT: number = 60;

export const DEFAULT_MAIN_DEPTH: DepthCurve = {
  name: 'MD',
  color: '#021D38',
  floatingPoint: 0,
};
